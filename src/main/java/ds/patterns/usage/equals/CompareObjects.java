package ds.patterns.usage.equals;

public class CompareObjects {
    /**
     *    if (strA == strB)
     *      == compares object references, it checks to see if the two operands point to the same object.
     *
     *      Note: This does not check for equivalency of the two objects but the checks if they are the same objects
     *            Compares the memory references of the two objects.
     *
     *
     *    if (strA.equals(strB))
     *      .equals compares the contents of the two string, to check if they are equivalent, if they contain the same characters
     *
     **/

}
